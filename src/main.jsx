import React from "react";
import ReactDOM from "react-dom/client";
import SalesGamificationApp from "./SalesGamificationApp";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SalesGamificationApp />
  </React.StrictMode>
);
